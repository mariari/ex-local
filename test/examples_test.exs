defmodule EUploadTest do
  use ExExample.ExUnit, for: EUpload
end

defmodule ECommentTest do
  use ExExample.ExUnit, for: EComment
end

defmodule EVoteTest do
  use ExExample.ExUnit, for: EVote
end

defmodule EEventStoreTest do
  use ExExample.ExUnit, for: EEventStore
end

defmodule EDeleteTest do
  use ExExample.ExUnit, for: EDelete
end

defmodule EWebUITest do
  use ExExample.ExUnit, for: EWebUI
end

defmodule EPomfResponseTest do
  use ExExample.ExUnit, for: EPomfResponse
end

defmodule EPomfRoundTripTest do
  use ExExample.ExUnit, for: EPomfRoundTrip
end

defmodule ESecretTest do
  use ExExample.ExUnit, for: ESecret
end
